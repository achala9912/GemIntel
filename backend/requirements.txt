# --- Web Server & API ---
fastapi==0.103.1
uvicorn==0.23.2
python-multipart==0.0.6

# --- Core Data & Image Processing ---
numpy==2.0.2
pandas==2.2.2
pillow==11.3.0
opencv-python-headless==4.13.0.92  # Uncomment if you switch backend image processing from Pillow to CV2
# albumentations==2.0.8           # Uncomment if you switch backend preprocessing from torchvision to Albumentations

# --- ML Pipeline (Crucial Version Matches) ---
scikit-learn==1.6.1
scikit-image==0.25.2
xgboost==3.2.0
joblib==1.3.2

# --- Deep Learning (PyTorch CPU + timm) ---
# We force the CPU version to keep the Docker container small and fast for web servers
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.10.0
torchvision
timm